# DESIGN

Cartographic studio. Cool paper (`#eef0ec`), ink (`#17191b`), one survey-green accent (`#1f6b4a`). Outfit for type, IBM Plex Mono for coordinates. Sharp corners. Split heroes. Motion only for section reveal.

No purple gradients, no Inter, no em-dashes in UI copy, one CTA intent: Book a search audit.
